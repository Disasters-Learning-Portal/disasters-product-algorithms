algorithm_name: capella
# git ref DPS clones (branch or tag). 'dev' tracks active development; pin a tag
# (e.g. v0.10.0) for reproducible production runs.
algorithm_version: dev
algorithm_description: >-
  Process Capella SAR scenes into Cloud Optimized GeoTIFF disaster-response
  products. Fetches the source GEO rasters from the CSDA Capella vendor S3
  bucket (keyed by date/bucket/prefix), computes the sigma-naught calibration
  product, optionally applies a Lee speckle filter, and writes a COG with
  activation-event GeoTIFF metadata tags.
code_repository: https://github.com/Disasters-Learning-Portal/disasters-product-algorithms.git
# MAAP clones the repo to /app/<repo-name>/ and runs these from /app, so the
# command paths MUST be prefixed with the repo directory name.
build_command: disasters-product-algorithms/dps/capella/build-env.sh
run_command: disasters-product-algorithms/dps/capella/run.sh
# MAAP OPS default base image. MAAP installs Miniconda during the build and runs
# build-env.sh on top, so a minimal base is fine.
base_container_url: mas.maap-project.org/root/maap-workspaces/custom_images/maap_base:v4.2.0
# Resource requirements. SAR scenes are smaller than optical mosaics.
ram_min: 32
cores_min: 4
outdir_max: 20
# Metadata (pre-fills the registration form).
author: NASA Disasters
contributor: NASA Disasters
license: Apache-2.0
release_notes: DPS integration for Capella SAR sigma0 COG disaster-product generation.
citation: NASA Disasters Program
keywords: capella, sar, sigma0, cog, disasters, flood
# AWS read access: NO file input -- the CLI FETCHES source rasters from the CSDA
# vendor bucket (default csdap-capellaspace-delivery, prefix 'disasters') at run
# time. The DPS-worker role needs read access to that bucket (confirmed available).
inputs:
  - name: date
    label: Target date
    doc: "Target acquisition date, YYYYMMDDHHMMSS. The closest matching Capella scene in the vendor bucket is selected. To discover valid dates, run the standalone 'list-dates' discovery algorithm (sensor=capella) or use: aws s3 ls s3://csdap-capellaspace-delivery/disasters/ (see docs/DPS.md 'Finding available scenes')."
    type: string
  - name: filter_size
    label: Lee filter window size
    doc: "Lee speckle-filter window size in pixels. Filtering is always applied; this only tunes the kernel. Must be 3, 5 or 7."
    type: int
    default: 5
  - name: activation_event
    label: Activation event
    doc: "Activation event, e.g. 202511_Flood_TX. The placeholder YYYYMM_Hazard_Location is rejected at run time -- you MUST set a real value."
    type: string
    default: "YYYYMM_Hazard_Location"
  - name: source_label
    label: Source
    doc: "Data origin (required), e.g. USGS, NASA, NOAA, Copernicus, Capella Space."
    type: string
outputs:
  - name: output
    type: Directory
